{
  "name": "dotgui-figma",
  "private": true,
  "scripts": {
    "build": "bun run build:ui && bun run build:plugin",
    "build:plugin": "bun build src/code.ts --outfile dist/code.js --target browser",
    "build:ui": "vite build",
    "dev": "vite build --watch"
  },
  "dependencies": {
    "dotgui-render": "file:../dotgui-render",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.101.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vite-plugin-singlefile": "^2.0.2"
  }
}
